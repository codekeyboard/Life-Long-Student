## COmopse project in practice/docker-compose

`docker compose build`

`docker compose up`

`docker compose ps` 

`docker compose logs -f web-fe`

## Compose V2 commands

`docker compose ls`

`docker compoe up -d` it will not start new version


## Clean Up
for new version 
first change the port

`docker compose -p test up -d` 

`docker compose down`

`docker compose -p test down`
