Difference btw real doma nd virtual dom :
Real DOM:
IN real DOM as in javascript web when a change trigger it have to reload all the DOM that can cause problem. 

to solve this porblem we  have to go to virtauul DOM in REACT in which the specific component will get reload. 

it compare with real dom and any changed that ocured will be applyed by virtual DOM react.

JSX:

JAvascript HTML

combination of js and html we can write html in JS.

Setting up react with vite react + vite

If have to render different data but using same component again and again than we have ti use Props Drilling.


data alway goes from up to down never goes from down to up like App.jsx is  parent than it child like a component Card.jsx wantt data it will get through its parent App.jsx 
